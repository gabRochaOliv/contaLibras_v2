---
phase: 01-feedback-api
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-04
autonomous: false
files_modified:
  - api/main.py
  - conta_libras_2/lib/core/constants.dart
requirements:
  - D-06
  - D-07
  - D-08
  - D-09

user_setup:
  - service: supabase
    why: "Banco de dados PostgreSQL para persistência dos feedbacks"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard > Project Settings > Database > Connection string > URI (modo Direct connection, porta 5432)"
    dashboard_config:
      - task: "Criar projeto Supabase"
        location: "supabase.com/dashboard > New Project"
      - task: "Executar o SQL de criação da tabela feedbacks"
        location: "Supabase Dashboard > SQL Editor > New query (colar o SQL fornecido na tarefa)"
  - service: railway
    why: "Deploy da API FastAPI"
    env_vars:
      - name: DATABASE_URL
        source: "Mesmo valor do Supabase — configurar em Railway > Project > Variables"
    dashboard_config:
      - task: "Criar projeto Railway conectado ao repositório GitHub"
        location: "railway.com/new > Deploy from GitHub repo > selecionar pasta api/"
      - task: "Configurar variável DATABASE_URL"
        location: "Railway > Project > Variables > Add variable"
      - task: "Anotar a URL pública gerada pelo Railway"
        location: "Railway > Project > Settings > Networking > Public domain"

must_haves:
  truths:
    - "A tabela feedbacks existe no Supabase com colunas id, nome, idade, categoria, respostas (jsonb), criado_em"
    - "A API está rodando no Railway e GET /health retorna {\"status\": \"ok\"}"
    - "POST /feedback para a URL Railway com payload válido retorna 201 e insere uma linha na tabela feedbacks"
    - "AppConstants.apiBaseUrl aponta para a URL Railway real (não o placeholder)"
    - "O app Flutter em modo release envia dados reais para a API Railway"
  artifacts:
    - path: "api/main.py"
      provides: "CORSMiddleware atualizado com URL Vercel do app"
      contains: "allow_origins"
    - path: "conta_libras_2/lib/core/constants.dart"
      provides: "URL Railway real no getter apiBaseUrl"
      contains: "railway.app"
  key_links:
    - from: "Flutter app (Vercel)"
      to: "API Railway"
      via: "POST https://{url-railway}/feedback"
      pattern: "POST.*railway.*feedback"
    - from: "API Railway"
      to: "PostgreSQL Supabase"
      via: "psycopg2 connection string porta 5432"
      pattern: "DATABASE_URL.*supabase"
---

<objective>
Configurar a infraestrutura de deploy: criar banco PostgreSQL no Supabase, executar o SQL de criação da tabela, fazer deploy da API no Railway, e atualizar AppConstants e CORS com as URLs reais.

Purpose: Este é o plano de integração final — une os artefatos criados nos planos anteriores com a infraestrutura real. Requer ações humanas (criar conta Supabase, criar conta Railway, configurar variáveis) e ações do executor (atualizar URLs no código após confirmação humana).
Output: API rodando no Railway com banco Supabase funcional; AppConstants com URL real; CORS com URL Vercel.
</objective>

<execution_context>
@Z:/Users/bielr/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Users/bielr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\STATE.md
@Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-04-SUMMARY.md

<interfaces>
<!-- SQL para criação da tabela — executar no Supabase SQL Editor -->
```sql
CREATE TABLE IF NOT EXISTS feedbacks (
    id         SERIAL PRIMARY KEY,
    nome       VARCHAR(255)  NOT NULL,
    idade      INTEGER       NOT NULL,
    categoria  VARCHAR(100)  NOT NULL,
    respostas  JSONB         NOT NULL,
    criado_em  TIMESTAMPTZ   NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_feedbacks_categoria ON feedbacks(categoria);
CREATE INDEX IF NOT EXISTS idx_feedbacks_criado_em ON feedbacks(criado_em);
```

<!-- Formato da DATABASE_URL do Supabase (Direct connection, porta 5432) -->
postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

<!-- Procfile existente em api/Procfile -->
web: uvicorn main:app --host 0.0.0.0 --port $PORT
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Tarefa 1: Setup Supabase e Railway</name>
  <what-built>
    Os arquivos api/ foram criados no Plan 02. A API está pronta para deploy — agora é necessário provisionar a infraestrutura.
  </what-built>
  <how-to-verify>
    Execute cada passo na ordem abaixo e anote as informações solicitadas:

    PASSO 1 — Supabase (banco de dados):
    1. Acesse supabase.com e crie uma conta (ou faça login)
    2. Crie um novo projeto (nome sugerido: "conta-libras-tcc")
    3. Aguarde o projeto ser provisionado (pode levar 1–2 min)
    4. Vá em: Project Settings > Database > Connection string > URI
    5. Selecione "Direct connection" e copie a string (formato: postgresql://postgres:PASSWORD@db.REF.supabase.co:5432/postgres)
    6. Vá em: SQL Editor > New query
    7. Cole e execute o SQL abaixo:

       CREATE TABLE IF NOT EXISTS feedbacks (
           id         SERIAL PRIMARY KEY,
           nome       VARCHAR(255)  NOT NULL,
           idade      INTEGER       NOT NULL,
           categoria  VARCHAR(100)  NOT NULL,
           respostas  JSONB         NOT NULL,
           criado_em  TIMESTAMPTZ   NOT NULL DEFAULT NOW()
       );
       CREATE INDEX IF NOT EXISTS idx_feedbacks_categoria ON feedbacks(categoria);
       CREATE INDEX IF NOT EXISTS idx_feedbacks_criado_em ON feedbacks(criado_em);

    8. Confirme que a query retornou "Success. No rows returned."
    9. Anote a DATABASE_URL (string de conexão copiada no passo 5)

    PASSO 2 — Railway (deploy da API):
    1. Acesse railway.com e crie uma conta (ou faça login)
    2. Clique em "New Project" > "Deploy from GitHub repo"
    3. Selecione o repositório do TCC
    4. Configure o "Root Directory" para: api/ (se Railway pedir)
    5. Adicione a variável de ambiente: DATABASE_URL = [valor copiado do Supabase]
    6. O Railway vai detectar o runtime.txt e o Procfile automaticamente e fazer o deploy
    7. Aguarde o deploy completar (2–5 min)
    8. Vá em: Project > Settings > Networking > Public domain
    9. Copie a URL pública gerada (formato: https://api-something.railway.app ou similar)
    10. Confirme que GET https://[SUA_URL_RAILWAY]/health retorna {"status": "ok"}

    PASSO 3 — URL Vercel do app:
    1. Acesse vercel.com e abra o projeto do ContaLibras
    2. Copie a URL de produção (formato: https://conta-libras-XXXX.vercel.app)

    Após concluir, forneça:
    - URL Railway da API (ex: https://api-something.up.railway.app)
    - URL Vercel do app (ex: https://conta-libras-xxxx.vercel.app)
    - Confirme que /health retorna {"status": "ok"}
  </how-to-verify>
  <resume-signal>
    Informe as URLs Railway e Vercel, e confirme que o /health está respondendo.
    Exemplo: "Railway: https://minha-api.railway.app | Vercel: https://conta-libras.vercel.app | health: ok"
  </resume-signal>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar URLs — AppConstants e CORS</name>
  <files>conta_libras_2/lib/core/constants.dart, api/main.py</files>
  <read_first>
    - conta_libras_2/lib/core/constants.dart (ler estado atual antes de editar)
    - api/main.py (ler allow_origins atual antes de editar)
  </read_first>
  <action>
    Esta tarefa executa APÓS o usuário fornecer as URLs na Tarefa 1.

    Em conta_libras_2/lib/core/constants.dart:
    - Substituir o placeholder 'https://api-conta-libras.railway.app' pela URL Railway real fornecida pelo usuário
    - Manter a lógica kDebugMode — apenas a URL de produção muda
    - Verificar que a URL não tem barra final (trailing slash) — o FeedbackService concatena '/feedback' diretamente

    Em api/main.py:
    - Substituir allow_origins=["*"] por allow_origins=["URL_VERCEL_DO_USUARIO", "http://localhost:*"]
    - Exemplo: allow_origins=["https://conta-libras-xxxx.vercel.app", "http://localhost:*"]
    - Usar a URL Vercel exata fornecida pelo usuário — sem wildcard de subdomínio
    - Após editar main.py, fazer commit e push para o repositório; o Railway vai redeploy automaticamente

    Após as edições:
    1. cd conta_libras_2 && flutter analyze lib/core/constants.dart (verificar zero erros)
    2. Fazer git commit e push dos arquivos modificados para trigger do redeploy Railway
  </action>
  <verify>
    <automated>cd conta_libras_2 && flutter analyze lib/core/constants.dart && echo "constants OK"</automated>
  </verify>
  <acceptance_criteria>
    - constants.dart contém a URL Railway real (não o placeholder 'api-conta-libras.railway.app')
    - constants.dart NÃO termina a URL com "/" (sem trailing slash)
    - api/main.py allow_origins contém a URL Vercel real do usuário (não ["*"])
    - api/main.py allow_origins ainda inclui "http://localhost:*" para desenvolvimento local
    - flutter analyze lib/core/constants.dart retorna zero erros
    - GET https://[URL_RAILWAY]/health retorna {"status": "ok"} após redeploy (verificar com curl ou browser)
  </acceptance_criteria>
  <done>AppConstants aponta para a API Railway real. CORS permite chamadas do domínio Vercel específico. Sistema completo e integrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Developer machine → Supabase | DATABASE_URL copiada manualmente — nunca commitada no git |
| Railway → Supabase | Variável de ambiente Railway — não exposta no código |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | DATABASE_URL | mitigate | Apenas em variável Railway; .env não commitado; .env.example contém placeholder — verificar .gitignore inclui .env |
| T-05-02 | Spoofing | CORS allow_origins | mitigate | Atualizar de ["*"] para URL Vercel específica neste plano — reduz superfície de requisições cross-origin indesejadas |
| T-05-03 | Tampering | SQL de criação da tabela | accept | SQL executado manualmente pelo dev no dashboard Supabase — sem risco de injeção nessa etapa |
</threat_model>

<verification>
- GET https://[URL_RAILWAY]/health retorna 200 {"status": "ok"}
- curl -X POST https://[URL_RAILWAY]/feedback -H "Content-Type: application/json" -d '{"nome":"Teste","idade":25,"categoria":"Estudante","respostas":[{"pergunta_id":4,"valor":5}],"timestamp":"2026-06-04T12:00:00Z"}' retorna 201
- Consulta no Supabase SQL Editor: SELECT * FROM feedbacks ORDER BY criado_em DESC LIMIT 1; retorna a linha inserida
- constants.dart: grep "railway.app" conta_libras_2/lib/core/constants.dart retorna a URL real (não placeholder)
</verification>

<success_criteria>
A tabela feedbacks existe no Supabase. A API está rodando no Railway com /health respondendo. POST /feedback persiste dados reais. AppConstants aponta para a URL Railway correta. CORS configurado com URL Vercel específica. O loop completo Flutter → Railway → Supabase está funcional.
</success_criteria>

<output>
Criar .planning/phases/01-feedback-api/01-05-SUMMARY.md quando concluído.
</output>
