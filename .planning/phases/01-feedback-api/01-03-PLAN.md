---
phase: 01-feedback-api
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - conta_libras_2/lib/data/services/feedback_service.dart
  - conta_libras_2/test/feedback_service_test.dart
autonomous: true
requirements:
  - D-08
  - D-10
  - D-12
  - D-13

must_haves:
  truths:
    - "FeedbackService é um singleton ChangeNotifier com getters isLoading e hasError"
    - "FeedbackService.submit() monta o payload com nome, idade, categoria, respostas e timestamp e faz POST para AppConstants.apiBaseUrl/feedback"
    - "Após submit() bem-sucedido, isLoading é false e hasError é false"
    - "Após submit() com erro de rede ou resposta não-2xx, isLoading é false e hasError é true"
    - "O dialog não é fechado pelo FeedbackService em caso de erro — a decisão de fechar ou não é do caller (D-13)"
  artifacts:
    - path: "conta_libras_2/lib/data/services/feedback_service.dart"
      provides: "Serviço HTTP singleton para envio de feedbacks"
      exports: ["FeedbackService"]
      contains: "class FeedbackService extends ChangeNotifier"
    - path: "conta_libras_2/test/feedback_service_test.dart"
      provides: "Testes unitários do FeedbackService"
      contains: "test_submit_success"
  key_links:
    - from: "conta_libras_2/lib/data/services/feedback_service.dart"
      to: "conta_libras_2/lib/core/constants.dart"
      via: "AppConstants.apiBaseUrl"
      pattern: "AppConstants\\.apiBaseUrl"
    - from: "conta_libras_2/lib/data/services/feedback_service.dart"
      to: "conta_libras_2/lib/data/managers/user_manager.dart"
      via: "UserManager().userName, UserManager().userAge, UserManager().userCategory"
      pattern: "UserManager\\(\\)"
---

<objective>
Criar o FeedbackService — serviço singleton ChangeNotifier que encapsula o HTTP POST para a API Railway, monta o payload JSON completo e expõe estado de loading/error para o EvaluationDialog.

Purpose: O EvaluationDialog (Plan 05) depende desse serviço para enviar os dados sem precisar conhecer detalhes de HTTP. Separar a lógica HTTP do widget segue o padrão singleton estabelecido pelos managers do projeto.
Output: feedback_service.dart criado seguindo o padrão de user_manager.dart; testes unitários mock do HTTP.
</objective>

<execution_context>
@Z:/Users/bielr/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Users/bielr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\STATE.md
@Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-01-SUMMARY.md

<interfaces>
<!-- Contratos que o FeedbackService deve satisfazer — extraídos do PATTERNS.md e RESEARCH.md -->

Padrão singleton ChangeNotifier (de user_manager.dart):
```dart
import 'package:flutter/foundation.dart';

class UserManager extends ChangeNotifier {
  static final UserManager _instance = UserManager._internal();
  factory UserManager() => _instance;
  UserManager._internal();
  // campos, getters, métodos
}
```

UserManager após Plan 01-01 (campos disponíveis para o payload):
```dart
String get userName => _userName;      // nome do usuário
String get userCategory => _userCategory; // categoria
int get userAge => _userAge;            // idade (adicionado em 01-01)
```

AppConstants após Plan 01-01:
```dart
static String get apiBaseUrl => kDebugMode
    ? 'http://localhost:8000'
    : 'https://api-conta-libras.railway.app';
```

Interface esperada pelo EvaluationDialog (caller):
```dart
final service = FeedbackService();
await service.submit(_answers);  // _answers: Map<int, int> — chave = pergunta_id, valor = 1-5
if (service.hasError) {
  // mostrar banner de erro, NÃO fechar dialog
} else {
  Navigator.of(context).pop();
  // mostrar snackbar de sucesso
}
```

Payload JSON a ser montado (D-10):
```
{
  "nome": UserManager().userName,
  "idade": UserManager().userAge,
  "categoria": UserManager().userCategory,
  "respostas": [ {"pergunta_id": key, "valor": value} for each entry in answers ],
  "timestamp": DateTime.now().toIso8601String()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar FeedbackService e testes unitários</name>
  <files>conta_libras_2/lib/data/services/feedback_service.dart, conta_libras_2/test/feedback_service_test.dart</files>
  <read_first>
    - conta_libras_2/lib/data/managers/user_manager.dart (padrão singleton ChangeNotifier a replicar)
    - conta_libras_2/lib/data/managers/progress_manager.dart (segundo exemplo do padrão — verificar estrutura)
    - conta_libras_2/lib/core/constants.dart (criado em 01-01 — verificar getter apiBaseUrl)
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-PATTERNS.md seção "feedback_service.dart (NOVO)"
  </read_first>
  <behavior>
    - Test 1: Antes de qualquer submit, isLoading == false e hasError == false
    - Test 2: Durante submit em-flight (mockado com delay), isLoading == true
    - Test 3: Após submit com resposta HTTP 201, isLoading == false e hasError == false
    - Test 4: Após submit com resposta HTTP 500, isLoading == false e hasError == true
    - Test 5: Após submit com SocketException (sem rede), isLoading == false e hasError == true
    - Test 6: O payload enviado contém as chaves "nome", "idade", "categoria", "respostas", "timestamp"
  </behavior>
  <action>
    Criar conta_libras_2/lib/data/services/feedback_service.dart:
    - Localização: lib/data/services/ (criar diretório services/ se não existir)
    - Imports: dart:convert; package:flutter/foundation.dart; package:http/http.dart as http; ../managers/user_manager.dart; ../../core/constants.dart
    - Classe FeedbackService extends ChangeNotifier com padrão singleton idêntico ao UserManager (static final _instance, factory, _internal())
    - Campos privados: bool _isLoading = false; bool _hasError = false; String? _errorMessage;
    - Getters públicos: bool get isLoading, bool get hasError, String? get errorMessage
    - Método submit(Map<int, int> answers) async:
      1. _isLoading = true; _hasError = false; _errorMessage = null; notifyListeners();
      2. Montar payload com UserManager().userName, userAge, userCategory, answers.entries.map((e) => {'pergunta_id': e.key, 'valor': e.value}).toList(), DateTime.now().toIso8601String()
      3. try: http.post(Uri.parse('${AppConstants.apiBaseUrl}/feedback'), headers: {'Content-Type': 'application/json; charset=UTF-8'}, body: jsonEncode(payload))
      4. Se statusCode < 200 ou >= 300: throw Exception('HTTP ${response.statusCode}')
      5. catch (e): _hasError = true; _errorMessage = 'Erro ao enviar avaliação. Verifique sua conexão.'
      6. finally: _isLoading = false; notifyListeners();
    - NÃO chamar Navigator.pop() ou qualquer operação de UI dentro do serviço — a responsabilidade de fechar o dialog é do EvaluationDialog (D-13)

    Criar conta_libras_2/test/feedback_service_test.dart:
    - Usar flutter_test (já no dev_dependencies) e mockito ou injeção direta de http.Client mock
    - Abordagem recomendada (sem mockito): criar subclasse de http.BaseClient para simular respostas, ou usar MockClient do pacote http_testing se disponível
    - Alternativa simples sem dependência extra: usar a técnica de passar um http.Client como parâmetro opcional ao submit para injeção em teste:
      Future<void> submit(Map<int, int> answers, {http.Client? httpClient}) async { ... }
      Em produção, o submit usa http.Client() padrão; nos testes, passa-se um MockClient.
    - Implementar os 6 casos de teste do behavior usando MockClient que retorna Response com status configurável
    - Testar isoladamente sem depender da URL Railway real
  </action>
  <verify>
    <automated>cd conta_libras_2 && flutter test test/feedback_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo conta_libras_2/lib/data/services/feedback_service.dart existe
    - feedback_service.dart contém "class FeedbackService extends ChangeNotifier"
    - feedback_service.dart contém padrão singleton: "static final FeedbackService _instance = FeedbackService._internal();"
    - feedback_service.dart contém "bool get isLoading" e "bool get hasError"
    - feedback_service.dart contém "Future<void> submit(Map<int, int> answers"
    - feedback_service.dart NÃO contém "Navigator" (sem lógica de UI no serviço)
    - feedback_service.dart usa jsonEncode para serializar o payload
    - flutter test test/feedback_service_test.dart termina com todos os testes PASS (mínimo 4 testes passando)
    - flutter analyze lib/data/services/feedback_service.dart retorna zero erros
  </acceptance_criteria>
  <done>FeedbackService disponível para import pelo EvaluationDialog. Singleton com isLoading/hasError testado. Payload montado corretamente com os campos do D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| FeedbackService → API Railway | Dado do usuário serializado como JSON; Content-Type correto previne rejeição CORS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | errorMessage exposto via getter | accept | Mensagem de erro é genérica ("Verifique sua conexão") — não expõe detalhes internos da API |
| T-03-02 | Tampering | Payload JSON | accept | FeedbackService monta o payload de fontes confiáveis (UserManager em memória); sem input externo não-sanitizado |
</threat_model>

<verification>
- flutter test test/feedback_service_test.dart — todos os testes PASS
- flutter analyze lib/data/services/feedback_service.dart — zero erros
- grep "Navigator" conta_libras_2/lib/data/services/feedback_service.dart deve retornar vazio (zero ocorrências)
- grep "jsonEncode" conta_libras_2/lib/data/services/feedback_service.dart retorna resultado
</verification>

<success_criteria>
FeedbackService criado como singleton ChangeNotifier, expõe isLoading e hasError, monta payload completo conforme D-10, faz POST para AppConstants.apiBaseUrl/feedback, e não tem lógica de UI (D-13). Testes unitários passando com HTTP mockado.
</success_criteria>

<output>
Criar .planning/phases/01-feedback-api/01-03-SUMMARY.md quando concluído.
</output>
