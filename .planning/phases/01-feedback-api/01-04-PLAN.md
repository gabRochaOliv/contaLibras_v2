---
phase: 01-feedback-api
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - conta_libras_2/lib/ui/widgets/evaluation_dialog.dart
  - conta_libras_2/test/evaluation_dialog_test.dart
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "EvaluationDialog exibe o TCLE como primeiro passo e avança para as seções somente após aceite"
    - "O questionário navega por 6 seções comuns (IDs 4–29) com barra de progresso entre seções"
    - "Cada seção exibe todas as suas perguntas Likert verticalmente num SingleChildScrollView com botões Voltar/Próximo"
    - "O botão Próximo/Enviar Avaliação está desabilitado enquanto nem todas as perguntas da seção têm resposta"
    - "Após as 6 seções comuns, usuários com categoria diferente de Outro veem a Seção 7 com perguntas específicas da categoria"
    - "Usuários com categoria Outro submetem ao fim da seção 6 sem ver seção adicional"
    - "O auto-advance por clique Likert está removido — clique só registra a resposta"
  artifacts:
    - path: "conta_libras_2/lib/ui/widgets/evaluation_dialog.dart"
      provides: "EvaluationDialog refatorado com navegação por seções, 26+ perguntas Likert, perguntas por categoria"
      contains: "_sections"
    - path: "conta_libras_2/test/evaluation_dialog_test.dart"
      provides: "Testes de widget para o fluxo de navegação"
      contains: "test_section_gate"
  key_links:
    - from: "conta_libras_2/lib/ui/widgets/evaluation_dialog.dart"
      to: "conta_libras_2/lib/data/services/feedback_service.dart"
      via: "FeedbackService().submit(_answers)"
      pattern: "FeedbackService\\(\\)\\.submit"
    - from: "conta_libras_2/lib/ui/widgets/evaluation_dialog.dart"
      to: "conta_libras_2/lib/data/managers/user_manager.dart"
      via: "UserManager().userCategory para branching de seção por categoria"
      pattern: "UserManager\\(\\)\\.userCategory"
---

<objective>
Refatorar o EvaluationDialog: substituir as 3 perguntas individuais com autonext pela navegação por seções agrupadas com as 26 perguntas comuns (IDs 4–29) e perguntas específicas por categoria (IDs 30–41). Implementar gate de seção completa, barra de progresso, estados de loading/error na submissão.

Purpose: Este é o principal entregável do app Flutter nesta fase — o questionário completo que os participantes do TCC responderão. Depende de FeedbackService (Plan 03) e UserManager com age (Plan 01).
Output: evaluation_dialog.dart totalmente reescrito segundo o UI-SPEC; testes de widget cobrindo fluxo de navegação e erro de submissão.
</objective>

<execution_context>
@Z:/Users/bielr/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Users/bielr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\STATE.md
@Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-03-SUMMARY.md
@Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-UI-SPEC.md

<interfaces>
<!-- Contratos extraídos do UI-SPEC e PATTERNS.md para uso direto pelo executor -->

Estrutura de seções comuns (_sections — IDs do questionario_IHC.md):
```dart
static const List<Map<String, dynamic>> _sections = [
  {'title': 'Usabilidade do Sistema (SUS)', 'icon': Icons.tune,
   'questions': [
     {'id': 4,  'text': 'Eu gostaria de utilizar este aplicativo com frequência.'},
     {'id': 5,  'text': 'O aplicativo é fácil de usar.'},
     {'id': 6,  'text': 'As funcionalidades do aplicativo são bem integradas.'},
     {'id': 7,  'text': 'A maioria das pessoas conseguiria aprender a usar este aplicativo rapidamente.'},
     {'id': 8,  'text': 'Navegar pelo aplicativo é simples e intuitivo.'},
     {'id': 9,  'text': 'Eu me senti confiante ao utilizar o aplicativo.'},
     {'id': 10, 'text': 'Não encontrei dificuldades significativas ao usar o aplicativo.'},
   ]},
  {'title': 'Experiência do Usuário (UX)', 'icon': Icons.star_outline,
   'questions': [
     {'id': 11, 'text': 'O design do aplicativo é agradável.'},
     {'id': 12, 'text': 'A organização das telas facilita o uso do aplicativo.'},
     {'id': 13, 'text': 'O aplicativo responde rapidamente às ações do usuário.'},
     {'id': 14, 'text': 'O aplicativo é visualmente claro e compreensível.'},
   ]},
  {'title': 'Qualidade do Conteúdo', 'icon': Icons.library_books_outlined,
   'questions': [
     {'id': 15, 'text': 'Os vídeos em Libras ajudam na compreensão dos termos apresentados.'},
     {'id': 16, 'text': 'As descrições escritas são claras e fáceis de entender.'},
     {'id': 17, 'text': 'O conteúdo apresentado é relevante para o aprendizado.'},
     {'id': 18, 'text': 'O aplicativo apresenta informações confiáveis.'},
   ]},
  {'title': 'Aprendizado', 'icon': Icons.school_outlined,
   'questions': [
     {'id': 19, 'text': 'O aplicativo contribuiu para meu aprendizado de Libras.'},
     {'id': 20, 'text': 'O aplicativo facilitou a compreensão de termos contábeis em Libras.'},
     {'id': 21, 'text': 'O aplicativo pode ser útil como ferramenta de apoio educacional.'},
     {'id': 22, 'text': 'O aplicativo pode ajudar na inclusão de pessoas surdas na área contábil.'},
   ]},
  {'title': 'Aceitação da Tecnologia (TAM)', 'icon': Icons.thumb_up_outlined,
   'questions': [
     {'id': 23, 'text': 'O aplicativo é útil para o aprendizado de Libras.'},
     {'id': 24, 'text': 'O aplicativo melhora o acesso ao conhecimento sobre contabilidade em Libras.'},
     {'id': 25, 'text': 'Eu recomendaria este aplicativo para outras pessoas.'},
     {'id': 26, 'text': 'Eu utilizaria este aplicativo novamente no futuro.'},
   ]},
  {'title': 'Avaliação Geral', 'icon': Icons.assessment_outlined,
   'questions': [
     {'id': 27, 'text': 'No geral, estou satisfeito com o aplicativo.'},
     {'id': 28, 'text': 'O aplicativo atende às expectativas dos usuários.'},
     {'id': 29, 'text': 'O aplicativo possui potencial para auxiliar no ensino de Libras.'},
   ]},
];
```

Perguntas específicas por categoria (_categoryQuestions — IDs 30–41, D-05):
```dart
static const Map<String, List<Map<String, dynamic>>> _categoryQuestions = {
  'Estudante': [
    {'id': 30, 'text': 'O aplicativo é útil como complemento aos estudos na área contábil.'},
    {'id': 31, 'text': 'Utilizaria o aplicativo para estudar termos contábeis em Libras.'},
    {'id': 32, 'text': 'O aplicativo facilitaria minha comunicação com colegas surdos.'},
  ],
  'Professor': [
    {'id': 33, 'text': 'Utilizaria o aplicativo como recurso pedagógico em sala de aula.'},
    {'id': 34, 'text': 'O conteúdo está adequado para uso em contexto educacional formal.'},
    {'id': 35, 'text': 'O aplicativo pode contribuir para a inclusão de alunos surdos.'},
  ],
  'Pessoa surda': [
    {'id': 36, 'text': 'Os sinais apresentados correspondem ao que conheço de Libras.'},
    {'id': 37, 'text': 'O aplicativo facilita minha compreensão de termos contábeis.'},
    {'id': 38, 'text': 'O aplicativo poderia me ajudar no mercado de trabalho na área contábil.'},
  ],
  'Intérprete de Libras': [
    {'id': 39, 'text': 'Os sinais apresentados são adequados para uso em contexto profissional.'},
    {'id': 40, 'text': 'O aplicativo pode ser útil para atualização do vocabulário técnico.'},
    {'id': 41, 'text': 'Recomendaria o uso deste aplicativo aos alunos que atendo.'},
  ],
};
```

Dimensões do dialog (UI-SPEC seção "Dialog Sizing"):
- TCLE: height = MediaQuery.of(context).size.height * 0.8, maxHeight: 650
- Seções: height = MediaQuery.of(context).size.height * 0.85, maxHeight: 700
- Width: 500, padding: 24 (preservar valores existentes)

Ícone para seção de perguntas específicas: Icons.person_outline
Título da seção específica: "Perguntas para {categoria}" (ex: "Perguntas para Estudantes" — adaptar plural conforme categoria)

Estado de erro visual (UI-SPEC "Submission Error Banner"):
- Container com Colors.red.shade50, Border.all(Colors.red.shade200), borderRadius 8, padding 12
- Icon: Icons.error_outline, Colors.red.shade600, size 18
- Texto: AppTextStyles.bodyLarge.copyWith(color: Colors.red.shade700)
- Copy: "Não foi possível enviar sua avaliação." + "Verifique sua conexão com a internet e tente novamente."
- TextButton "Tentar novamente" em AppColors.primary
- Semantics(liveRegion: true) no Container do banner

Estado de loading no botão Enviar (UI-SPEC "Submission Loading State"):
- onPressed: null quando isLoading
- Substituir texto por SizedBox(width: 20, height: 20, child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))

Gate de avanço de seção (PATTERNS.md):
- bool _isSectionComplete(List<Map<String, dynamic>> questions) =>
    questions.every((q) => _answers.containsKey(q['id'] as int));
- Botão desabilitado com disabledBackgroundColor: AppColors.primary.withOpacity(0.4)

Barra de progresso (UI-SPEC "Section Progress Indicator"):
- LinearProgressIndicator, minHeight: 6, backgroundColor: AppColors.divider, valueColor: AppColors.secondary
- Value: (currentSectionIndex + 1) / totalSections
- Label abaixo: "Seção N de M" right-aligned, AppTextStyles.label

Navegação (UI-SPEC "Navigation buttons"):
- "Voltar": OutlinedButton, foregroundColor e borderColor AppColors.primary, height 48px, borderRadius 12
- "Próximo" / "Enviar Avaliação": ElevatedButton, backgroundColor AppColors.primary, white text, height 48px, borderRadius 12
- Último botão na seção 6 para Outro: texto "Enviar Avaliação"
- Último botão na seção 7 para demais categorias: texto "Enviar Avaliação"

Acessibilidade (UI-SPEC "Accessibility"):
- Likert: Semantics(label: 'Opção $value de 5', child: GestureDetector(...))
- Header de seção: Semantics(header: true, child: Text(...))
- Banner de erro: Semantics(liveRegion: true, child: Container(...))
- Botão retry: Semantics(button: true, label: 'Tentar novamente enviar a avaliação', child: TextButton(...))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Refatorar EvaluationDialog — estrutura de dados, navegação por seções e UI das perguntas</name>
  <files>conta_libras_2/lib/ui/widgets/evaluation_dialog.dart</files>
  <read_first>
    - conta_libras_2/lib/ui/widgets/evaluation_dialog.dart (arquivo completo — estado atual com 316 linhas; ler antes de reescrever)
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-UI-SPEC.md (seções "Section Map", "Navigation and Interaction Contract", "Dialog Sizing", "New Components Required", "Color", "Accessibility")
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-PATTERNS.md seção "evaluation_dialog.dart"
    - conta_libras_2/lib/core/theme/app_colors.dart (referência de constantes de cor)
    - conta_libras_2/lib/core/theme/app_text_styles.dart (referência de estilos de texto)
  </read_first>
  <action>
    Reescrever evaluation_dialog.dart mantendo o mesmo nome de classe EvaluationDialog e StatefulWidget.

    PRESERVAR sem alteração:
    - Imports de app_colors.dart e app_text_styles.dart
    - Método _buildLikertScale(int questionIndex) — copiar sem mudanças (linhas 58–113 do original)
    - Método _buildTCLEView() completo — copiar sem mudanças (linhas 168–241 do original)
    - Helpers _buildSectionTitle(String) e _buildParagraph(String) — copiar sem mudanças
    - Dialog shell AnimatedContainer com shape, elevation, backgroundColor transparente

    ADICIONAR imports novos:
    - import '../../data/services/feedback_service.dart';
    - import '../../data/managers/user_manager.dart';

    SUBSTITUIR estado local:
    - Remover final List<String> _questions e as 3 perguntas hardcoded
    - Manter _pageController, _currentPage, _hasAcceptedTerms, _answers (Map<int, int>)
    - Adicionar bool _showError = false e bool _isSubmitting = false

    ADICIONAR constantes estáticas:
    - static const List<Map<String, dynamic>> _sections = [...] com as 6 seções (IDs 4–29 conforme interface acima)
    - static const Map<String, List<Map<String, dynamic>>> _categoryQuestions = {...} com as 4 categorias (IDs 30–41 conforme interface acima)
    - Categoria "Outro" não tem entrada no mapa — o branching usa um check de containsKey

    SUBSTITUIR _buildQuestionsView por _buildSectionPage(int sectionIndex):
    - Determinar a lista de questões da seção atual: se sectionIndex < _sections.length, usar _sections[sectionIndex]['questions']; senão, usar _categoryQuestions[UserManager().userCategory] ?? []
    - Column com: SectionHeaderRow, Divider, Expanded > SingleChildScrollView com questões, SizedBox(16), NavigationRow
    - SectionHeaderRow: Row com [Icon(sectionIcon, 20px, AppColors.secondary), SizedBox(8), Expanded(Text título, heading3), Text("Seção N de M", label, textSecondary)] — Semantics(header: true) no título
    - Para cada questão: Column com [Text(questão, bodyLarge bold), SizedBox(16), _buildLikertScale(q['id']), SizedBox(24)]
    - _buildLikertScale recebe o id global da pergunta como questionIndex (chave do Map<int,int> _answers)
    - NÃO usar autonext no _selectAnswer — remover o Future.delayed(_nextPage) existente; apenas setState(_answers[id] = value)
    - NavigationRow: Row com [if sectionIndex > 0: Expanded(OutlinedButton("Voltar", _goBack)), SizedBox(12), Expanded(ElevatedButton(onPressed: isSectionComplete ? _goForward : null, child: isLastSection ? "Enviar Avaliação" : "Próximo"))]
    - ElevatedButton disabled style: disabledBackgroundColor AppColors.primary.withOpacity(0.4), disabledForegroundColor Colors.white.withOpacity(0.6)

    ADICIONAR _buildSectionProgress():
    - Container com margin top 8, Column: [LinearProgressIndicator(minHeight:6, value: ..., backgroundColor: AppColors.divider, valueColor: AlwaysStoppedAnimation(AppColors.secondary)), SizedBox(4), Align(alignment: Alignment.centerRight, child: Text("Seção N de M", style: AppTextStyles.label))]
    - Calcular totalSections: _sections.length + (hasCategorySection ? 1 : 0) onde hasCategorySection = _categoryQuestions.containsKey(UserManager().userCategory)

    ATUALIZAR AnimatedContainer height:
    - height: _hasAcceptedTerms ? MediaQuery.of(context).size.height * 0.85 : MediaQuery.of(context).size.height * 0.8
    - constraints: BoxConstraints(maxHeight: _hasAcceptedTerms ? 700 : 650)

    ATUALIZAR build():
    - Quando _hasAcceptedTerms, mostrar Column com: [_buildSectionProgress(), const SizedBox(8), Expanded(PageView.builder(itemBuilder: (_, i) => _buildSectionPage(i)))]
    - PageView.builder itemCount = _sections.length + (hasCategorySection ? 1 : 0)
    - Quando !_hasAcceptedTerms, manter _buildTCLEView() sem alteração

    LÓGICA de navegação:
    - _goBack(): _pageController.previousPage(duration: 300ms, curve: Curves.easeInOut)
    - _goForward(): se não é última seção, _pageController.nextPage; se é última seção, chamar _submit()
    - isLastSection: _currentPage == (totalSections - 1)
    - isSectionComplete: _isSectionComplete(currentSectionQuestions)
    - bool _isSectionComplete(List questions) => questions.every((q) => _answers.containsKey(q['id'] as int))
  </action>
  <verify>
    <automated>cd conta_libras_2 && flutter analyze lib/ui/widgets/evaluation_dialog.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze lib/ui/widgets/evaluation_dialog.dart retorna zero erros
    - evaluation_dialog.dart contém "static const List<Map<String, dynamic>> _sections"
    - evaluation_dialog.dart contém "static const Map<String, List<Map<String, dynamic>>> _categoryQuestions"
    - evaluation_dialog.dart NÃO contém "Future.delayed" (autonext removido)
    - evaluation_dialog.dart contém "NeverScrollableScrollPhysics" no PageView
    - evaluation_dialog.dart contém "SingleChildScrollView" dentro do builder de seção
    - evaluation_dialog.dart contém "MediaQuery.of(context).size.height * 0.85" para modo seções
    - evaluation_dialog.dart contém "UserManager().userCategory" para branching de seção
    - Grep: grep -c "id.*30\|id.*31\|id.*32" evaluation_dialog.dart retorna resultado (perguntas categoria Estudante presentes)
    - evaluation_dialog.dart contém "Icons.tune" e "Icons.star_outline" e "Icons.assessment_outlined" (ícones de seção presentes)
  </acceptance_criteria>
  <done>EvaluationDialog renderiza as 6 seções comuns + seção de categoria, navega via botões, gate de seção funcionando, sem autonext.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar submissão, estados de loading/error e testes de widget</name>
  <files>conta_libras_2/lib/ui/widgets/evaluation_dialog.dart, conta_libras_2/test/evaluation_dialog_test.dart</files>
  <read_first>
    - conta_libras_2/lib/ui/widgets/evaluation_dialog.dart (recém refatorado na Tarefa 1 — ler estado atual)
    - conta_libras_2/lib/data/services/feedback_service.dart (criado no Plan 03 — verificar interface submit e getters)
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-UI-SPEC.md seções "Submission Loading State", "Submission Error Banner", "States and Transitions", "Copywriting Contract"
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-PATTERNS.md seção "Padrão de submissão — NOVO"
  </read_first>
  <behavior>
    - Test 1 (gate): Botão "Próximo" está desabilitado na seção 1 (SUS) se alguma das 7 perguntas não tem resposta
    - Test 2 (gate): Botão "Próximo" fica habilitado quando todas as 7 perguntas da seção SUS têm resposta
    - Test 3 (error banner): Após _submit() com FeedbackService.hasError == true, o dialog permanece aberto e exibe o banner de erro com texto "Não foi possível enviar"
    - Test 4 (retry): Após exibir banner de erro, tap em "Tentar novamente" dispara _submit() novamente
    - Test 5 (success): Após _submit() com FeedbackService.hasError == false, Navigator.pop() é chamado
  </behavior>
  <action>
    Adicionar em evaluation_dialog.dart — método _submit() async:
    1. setState(() { _isSubmitting = true; _showError = false; })
    2. final service = FeedbackService(); await service.submit(_answers);
    3. if (!mounted) return;
    4. if (service.hasError): setState(() { _isSubmitting = false; _showError = true; })
       // NÃO chamar Navigator.pop() em caso de erro (D-13)
    5. else: Navigator.of(context).pop(); ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text('Obrigado pela sua avaliação e contribuição à pesquisa!', style: AppTextStyles.bodyMedium.copyWith(color: Colors.white)), backgroundColor: AppColors.primary, behavior: SnackBarBehavior.floating))
    6. setState(() { _isSubmitting = false; }) no else (já coberto pelo if)

    Adicionar _buildErrorBanner() retornando o Container vermelho descrito na interface:
    - Visível apenas quando _showError == true
    - Semantics(liveRegion: true) envolvendo o Container
    - TextButton "Tentar novamente" com onPressed: () { setState(() { _showError = false; }); _submit(); }
    - Botão com Semantics(button: true, label: 'Tentar novamente enviar a avaliação')

    Atualizar NavigationRow na última seção:
    - ElevatedButton onPressed: quando _isSubmitting → null; quando seção completa e !_isSubmitting → _submit; quando seção incompleta → null
    - child: _isSubmitting ? SizedBox(width:20, height:20, child: CircularProgressIndicator(strokeWidth:2, color:Colors.white)) : Text("Enviar Avaliação")
    - Inserir _buildErrorBanner() antes da NavigationRow quando _showError == true

    Adicionar Semantics às escalas Likert dentro de _buildLikertScale:
    - Envolver cada GestureDetector com Semantics(label: 'Opção $value de 5', child: GestureDetector(...))

    Criar conta_libras_2/test/evaluation_dialog_test.dart:
    - Usar flutter_test e flutter/material.dart
    - Wrapper mínimo: MaterialApp > Scaffold > EvaluationDialog
    - Mockar FeedbackService para controlar hasError (injeção via parâmetro opcional ou override de factory em teste)
    - test_gate_disabled: pump dialog, aceitar TCLE, verificar que ElevatedButton("Próximo") está disabled (onPressed == null)
    - test_gate_enabled: pump dialog, aceitar TCLE, simular tap em todas as 7 escalas Likert da seção SUS, verificar que o botão fica enabled
    - test_error_banner: mockar FeedbackService com hasError=true, completar todas as seções, tap "Enviar Avaliação", pump, verificar presença de texto "Não foi possível enviar"
    - test_dialog_stays_open_on_error: após error, verificar que EvaluationDialog ainda está na árvore de widgets (Navigator.pop não foi chamado)
    - test_success_closes: mockar com hasError=false, completar e enviar, verificar Navigator.pop chamado
  </action>
  <verify>
    <automated>cd conta_libras_2 && flutter test test/evaluation_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - evaluation_dialog.dart contém "_submit()" como método async
    - evaluation_dialog.dart contém "FeedbackService().submit(_answers)" dentro de _submit
    - evaluation_dialog.dart contém "_showError" e "_isSubmitting" como campos de estado
    - evaluation_dialog.dart contém "CircularProgressIndicator" no botão de submit
    - evaluation_dialog.dart contém "Colors.red.shade50" no banner de erro
    - evaluation_dialog.dart contém "Tentar novamente" como texto do TextButton de retry
    - evaluation_dialog.dart contém "Semantics" pelo menos em 3 locais (Likert, header de seção, banner)
    - evaluation_dialog.dart NÃO contém "Navigator" dentro de qualquer handler de erro (apenas no handler de sucesso)
    - flutter test test/evaluation_dialog_test.dart termina com todos os testes PASS
    - flutter analyze lib/ui/widgets/evaluation_dialog.dart retorna zero erros
  </acceptance_criteria>
  <done>EvaluationDialog completo: submissão via FeedbackService, loading state no botão, banner de erro com retry, dialog permanece aberto em caso de falha (D-13), snackbar de sucesso, semântica acessível.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| UI → FeedbackService | Dialog passa Map<int,int> apenas com IDs conhecidos (do _sections e _categoryQuestions estáticos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | _answers Map<int,int> | accept | Chaves são IDs inteiros de perguntas estáticas; valores são inteiros 1–5 selecionados pelo usuário via Likert — sem input de texto livre |
| T-04-02 | Tampering | FeedbackService.submit — payload montado no serviço | accept | Dados vêm de UserManager (nome/categoria/idade) e _answers — sem concatenação SQL ou HTML |
</threat_model>

<verification>
- flutter test test/evaluation_dialog_test.dart — todos os testes PASS
- flutter analyze lib/ui/widgets/evaluation_dialog.dart — zero erros
- grep "_sections" conta_libras_2/lib/ui/widgets/evaluation_dialog.dart retorna resultado
- grep "Future.delayed" conta_libras_2/lib/ui/widgets/evaluation_dialog.dart retorna vazio (autonext removido)
- grep "_submit" conta_libras_2/lib/ui/widgets/evaluation_dialog.dart retorna resultado
- grep "Tentar novamente" conta_libras_2/lib/ui/widgets/evaluation_dialog.dart retorna resultado
</verification>

<success_criteria>
EvaluationDialog exibe todas as 6 seções comuns (26 questões, IDs 4–29), exibe seção 7 para categorias não-Outro (3 questões específicas, IDs 30+), navega via botões com gate de seção completa, submete via FeedbackService com loading/error states, e permanece aberto em caso de falha com opção de retry.
</success_criteria>

<output>
Criar .planning/phases/01-feedback-api/01-04-SUMMARY.md quando concluído.
</output>
