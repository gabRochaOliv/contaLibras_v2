---
phase: 01-feedback-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/main.py
  - api/models.py
  - api/database.py
  - api/requirements.txt
  - api/runtime.txt
  - api/Procfile
  - api/.env.example
  - api/tests/conftest.py
  - api/tests/test_feedback.py
autonomous: true
requirements:
  - D-06
  - D-07
  - D-09
  - D-10

must_haves:
  truths:
    - "POST /feedback com payload válido retorna HTTP 201 e persiste uma linha na tabela feedbacks"
    - "POST /feedback com payload inválido (campo ausente ou tipo errado) retorna HTTP 422 automaticamente via Pydantic"
    - "GET /health retorna {\"status\": \"ok\"} com HTTP 200"
    - "A API está configurada para rodar no Railway com Python 3.11 via runtime.txt e Procfile"
    - "DATABASE_URL nunca aparece no código-fonte — vem exclusivamente de variável de ambiente"
  artifacts:
    - path: "api/main.py"
      provides: "FastAPI app com endpoints POST /feedback e GET /health + CORSMiddleware"
      contains: "app = FastAPI"
    - path: "api/models.py"
      provides: "Schemas Pydantic para FeedbackPayload e Resposta"
      contains: "class FeedbackPayload(BaseModel)"
    - path: "api/database.py"
      provides: "Conexão psycopg2 ao Supabase e função insert_feedback"
      contains: "def insert_feedback"
    - path: "api/requirements.txt"
      provides: "Dependências Python para o Railway"
      contains: "fastapi"
    - path: "api/runtime.txt"
      provides: "Versão Python explícita para o Railway"
      contains: "python-3.11"
    - path: "api/Procfile"
      provides: "Comando de start para o Railway"
      contains: "uvicorn main:app"
    - path: "api/tests/test_feedback.py"
      provides: "Testes de integração do endpoint POST /feedback"
      contains: "test_feedback_valid_payload"
  key_links:
    - from: "api/main.py"
      to: "api/models.py"
      via: "from models import FeedbackPayload"
      pattern: "from models import FeedbackPayload"
    - from: "api/main.py"
      to: "api/database.py"
      via: "from database import insert_feedback"
      pattern: "from database import insert_feedback"
    - from: "api/database.py"
      to: "PostgreSQL Supabase"
      via: "psycopg2.connect(os.environ['DATABASE_URL']) porta 5432"
      pattern: "DATABASE_URL"
---

<objective>
Criar a API Python/FastAPI completa com endpoint POST /feedback, schema Pydantic, conexão psycopg2 ao Supabase, e toda a configuração de deploy no Railway.

Purpose: Esta é a camada backend que recebe os feedbacks do app Flutter. Sem ela, os dados do questionário não têm destino. Os arquivos são independentes do Flutter e podem ser desenvolvidos em paralelo (Wave 1).
Output: Diretório api/ com main.py, models.py, database.py, requirements.txt, runtime.txt, Procfile, .env.example, e testes pytest.
</objective>

<execution_context>
@Z:/Users/bielr/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Users/bielr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\STATE.md
@Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-RESEARCH.md

<interfaces>
<!-- Payload JSON esperado pelo endpoint POST /feedback (D-10): -->
```json
{
  "nome": "Ana",
  "idade": 22,
  "categoria": "Estudante",
  "respostas": [
    {"pergunta_id": 4, "valor": 5},
    {"pergunta_id": 29, "valor": 4}
  ],
  "timestamp": "2026-06-04T15:30:00.000Z"
}
```

<!-- Schema SQL da tabela feedbacks (executar no Supabase SQL Editor — Plan 06): -->
```sql
CREATE TABLE IF NOT EXISTS feedbacks (
    id         SERIAL PRIMARY KEY,
    nome       VARCHAR(255)  NOT NULL,
    idade      INTEGER       NOT NULL,
    categoria  VARCHAR(100)  NOT NULL,
    respostas  JSONB         NOT NULL,
    criado_em  TIMESTAMPTZ   NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_feedbacks_categoria ON feedbacks(categoria);
CREATE INDEX idx_feedbacks_criado_em ON feedbacks(criado_em);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar estrutura da API — main.py, models.py, database.py, arquivos de config</name>
  <files>api/main.py, api/models.py, api/database.py, api/requirements.txt, api/runtime.txt, api/Procfile, api/.env.example</files>
  <read_first>
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-RESEARCH.md seções "Pattern 3", "Pattern 4", "Pattern 5", "Pattern 6", "Common Pitfalls" e "Anti-Patterns to Avoid"
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-PATTERNS.md seções sobre api/main.py, api/database.py (no analog — usar padrões do RESEARCH.md)
  </read_first>
  <action>
    Criar diretório api/ na raiz do repositório (mesmo nível que conta_libras_2/).

    api/models.py — schema Pydantic v2:
    - Imports: from pydantic import BaseModel; from typing import List; from datetime import datetime
    - Classe Resposta(BaseModel): campos pergunta_id: int e valor: int (1-5)
    - Classe FeedbackPayload(BaseModel): campos nome: str, idade: int, categoria: str, respostas: List[Resposta], timestamp: datetime

    api/database.py — conexão psycopg2:
    - Imports: os, psycopg2, psycopg2.extras.Json, dotenv.load_dotenv
    - Chamar load_dotenv() no topo do módulo
    - Função get_connection(): retorna psycopg2.connect(os.environ["DATABASE_URL"]) — PORTA 5432 (evitar porta 6543 de transaction pooling — Pitfall 6)
    - Função insert_feedback(payload): abre conexão, executa INSERT parametrizado com %s placeholders (NUNCA f-string — Pitfall SQL injection), usa Json(respostas_json) do psycopg2.extras para o campo JSONB, chama conn.commit(), fecha conn em bloco finally
    - Pydantic v2: usar payload.respostas[i].model_dump() em vez de .dict() (PATTERNS.md nota)

    api/main.py — FastAPI app:
    - Criar app = FastAPI(title="ContaLibras Feedback API")
    - Adicionar CORSMiddleware ANTES das rotas com allow_origins=["*"] (será atualizado com URL Vercel no Plan 06), allow_methods=["POST", "GET"], allow_headers=["*"]
    - Rota GET /health retorna {"status": "ok"}
    - Rota POST /feedback com status_code=201, recebe FeedbackPayload, chama insert_feedback(payload), retorna {"message": "Feedback recebido com sucesso"}, trata exceções com HTTPException(status_code=500)

    api/requirements.txt:
    fastapi>=0.110.0,<1.0.0
    uvicorn[standard]>=0.29.0
    pydantic>=2.6.0,<3.0.0
    psycopg2-binary>=2.9.9
    python-dotenv>=1.0.0
    pytest>=7.0.0
    httpx>=0.24.0

    api/runtime.txt:
    python-3.11

    api/Procfile:
    web: uvicorn main:app --host 0.0.0.0 --port $PORT
    (--host 0.0.0.0 é obrigatório no Railway — sem ele a app não fica acessível externamente)

    api/.env.example:
    DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
    (placeholder — usuário preenche com a string real do Supabase após criar o banco no Plan 06)
  </action>
  <verify>
    <automated>cd api && python -c "from main import app; from models import FeedbackPayload; from database import insert_feedback; print('imports ok')" 2>&1 || echo "NOTA: se Python local for 3.7, teste de import vai falhar — isso e esperado. Verificar que os arquivos existem."</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 7 arquivos existem: api/main.py, api/models.py, api/database.py, api/requirements.txt, api/runtime.txt, api/Procfile, api/.env.example
    - api/models.py contém "class FeedbackPayload(BaseModel)" e "class Resposta(BaseModel)"
    - api/database.py contém "psycopg2.connect(os.environ[" — DATABASE_URL nunca hardcoded
    - api/database.py usa "%s" como placeholder no INSERT (não f-string)
    - api/database.py usa ".model_dump()" (Pydantic v2), não ".dict()"
    - api/main.py contém "app.add_middleware(CORSMiddleware" antes das definições de rotas
    - api/main.py contém "@app.post(\"/feedback\", status_code=201)"
    - api/runtime.txt contém exatamente "python-3.11"
    - api/Procfile contém "uvicorn main:app --host 0.0.0.0 --port $PORT"
  </acceptance_criteria>
  <done>Estrutura completa da API criada com os 7 arquivos. Pronta para testes e deploy.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Criar testes pytest para o endpoint POST /feedback</name>
  <files>api/tests/__init__.py, api/tests/conftest.py, api/tests/test_feedback.py</files>
  <read_first>
    - api/main.py (recém criado na Tarefa 1 — ler para entender os endpoints antes de escrever os testes)
    - api/models.py (recém criado — verificar campos obrigatórios do FeedbackPayload)
    - Z:\TCC\Aplicativo\.planning\phases\01-feedback-api\01-RESEARCH.md seção "Validation Architecture" (mapa de req ID → test)
  </read_first>
  <behavior>
    - Test 1 (test_health): GET /health retorna status 200 e corpo {"status": "ok"}
    - Test 2 (test_feedback_valid_payload): POST /feedback com payload completo válido retorna status 201 e body com chave "message" — banco mockado via monkeypatch em insert_feedback
    - Test 3 (test_feedback_missing_field): POST /feedback sem o campo "nome" retorna status 422 (Pydantic rejeita automaticamente)
    - Test 4 (test_feedback_invalid_valor): POST /feedback com "valor": "abc" (string em vez de int) retorna status 422
    - Test 5 (test_feedback_db_error): POST /feedback com insert_feedback lançando Exception retorna status 500
  </behavior>
  <action>
    Criar api/tests/__init__.py (vazio).

    api/tests/conftest.py:
    - Importar pytest e httpx
    - Importar app de main
    - Fixture client com escopo "module" usando httpx.AsyncClient(app=app, base_url="http://test") via pytest.fixture
    - Fixture valid_payload retornando dict com todos os campos: nome, idade, categoria, respostas (lista com 2 itens), timestamp como string ISO 8601

    api/tests/test_feedback.py:
    - Usar pytest e httpx; importar a fixture client e valid_payload de conftest
    - test_health: GET /health, assert status 200, assert response.json() == {"status": "ok"}
    - test_feedback_valid_payload: monkeypatch.setattr("database.insert_feedback", lambda p: None); POST /feedback com valid_payload; assert status 201; assert "message" in response.json()
    - test_feedback_missing_field: POST /feedback com payload sem campo "nome"; assert status 422
    - test_feedback_invalid_valor: POST /feedback com respostas=[{"pergunta_id": 4, "valor": "nao-e-int"}]; assert status 422
    - test_feedback_db_error: monkeypatch.setattr("database.insert_feedback", lambda p: (_ for _ in ()).throw(Exception("db error"))); POST /feedback; assert status 500

    Nota: testes são escritos para rodar no Railway (Python 3.11). Se o ambiente local tiver Python 3.7, o executor deve anotar isso no SUMMARY e executar os testes apenas após confirmar Python 3.11 disponível (via pyenv, conda, ou diretamente na CI do Railway).
  </action>
  <verify>
    <automated>cd api && python --version && python -m pytest tests/ -v 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - api/tests/test_feedback.py existe com as 5 funções de teste nomeadas conforme especificado
    - api/tests/conftest.py existe com fixture client e fixture valid_payload
    - Se Python >=3.11 disponível: pytest tests/ -v termina com "5 passed"
    - Se Python 3.7 local: os arquivos de teste existem e são sintaticamente corretos — confirmar com "python3.11 -m pytest tests/ -v" se disponível
  </acceptance_criteria>
  <done>Testes pytest criados cobrindo os cenários de D-06 mapeados no RESEARCH.md (payload válido → 201, inválido → 422, erro DB → 500).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Internet → POST /feedback | Payload não autenticado; validação via Pydantic é a única barreira |
| api/database.py → PostgreSQL Supabase | Credenciais via env var; nunca no código |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /feedback — injeção SQL | mitigate | Usar exclusivamente %s placeholders psycopg2; nunca f-string com dados do usuário |
| T-02-02 | Information Disclosure | DATABASE_URL | mitigate | Somente em variável de ambiente Railway; nunca commitado; .env.example com placeholder sem credenciais reais |
| T-02-03 | Spoofing | CORSMiddleware allow_origins=["*"] | accept | TCC sem cookies/credentials; allow_origins=["*"] sem allow_credentials=True é safe para browser; atualizar para URL Vercel específica no Plan 06 |
| T-02-04 | Denial of Service | POST /feedback — spam | accept | D-12 permite múltiplas submissões; volume TCC não justifica rate limiting |
| T-02-05 | Information Disclosure | HTTPException detail=str(e) | mitigate | Para TCC aceitável; em produção omitir detalhes internos — anotar no SUMMARY como limitação conhecida |
  T-02-SC | Tampering | pip install (PyPI packages) | mitigate | Todos os pacotes verificados manualmente no PyPI (RESEARCH.md Package Legitimacy Audit); disposição Aprovado para todos |
</threat_model>

<verification>
- Todos os 7 arquivos criados: api/main.py, api/models.py, api/database.py, api/requirements.txt, api/runtime.txt, api/Procfile, api/.env.example
- grep "model_dump" api/database.py retorna resultado (confirma Pydantic v2)
- grep "DATABASE_URL" api/database.py retorna resultado; grep "postgresql://" api/database.py não retorna resultado (URL nunca hardcoded)
- grep "%s" api/database.py retorna resultado (confirms parametrized query)
- api/tests/test_feedback.py existe com 5 funções de teste
</verification>

<success_criteria>
A API FastAPI está completa e pronta para deploy no Railway: endpoint POST /feedback valida o payload com Pydantic, persiste com psycopg2 via query parametrizada, retorna 201 em sucesso e 422/500 em erro. Testes pytest cobrem os cenários críticos. Nenhuma credencial hardcoded no código-fonte.
</success_criteria>

<output>
Criar .planning/phases/01-feedback-api/01-02-SUMMARY.md quando concluído.
</output>
