---
phase: 01-feedback-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conta_libras_2/pubspec.yaml
  - conta_libras_2/lib/data/managers/user_manager.dart
  - conta_libras_2/lib/ui/screens/first_access/first_access_screen.dart
  - conta_libras_2/lib/core/constants.dart
autonomous: true
requirements:
  - D-08
  - D-11

must_haves:
  truths:
    - "UserManager expõe o getter userAge (int) e aceita age como terceiro parâmetro em setUserData"
    - "FirstAccessScreen passa a idade digitada para UserManager.setUserData ao submeter o formulário"
    - "AppConstants.apiBaseUrl retorna http://localhost:8000 em kDebugMode e a URL Railway em modo release"
    - "O pacote http ^1.2.0 está declarado em pubspec.yaml"
  artifacts:
    - path: "conta_libras_2/lib/core/constants.dart"
      provides: "Constante de URL da API com diferenciação debug/release"
      contains: "class AppConstants"
    - path: "conta_libras_2/lib/data/managers/user_manager.dart"
      provides: "Getter userAge + assinatura setUserData atualizada"
      contains: "int get userAge"
    - path: "conta_libras_2/pubspec.yaml"
      provides: "Dependência http no projeto Flutter"
      contains: "http: ^1.2.0"
  key_links:
    - from: "conta_libras_2/lib/ui/screens/first_access/first_access_screen.dart"
      to: "conta_libras_2/lib/data/managers/user_manager.dart"
      via: "UserManager().setUserData(name, category, age)"
      pattern: "setUserData.*_ageController"
    - from: "conta_libras_2/lib/data/services/feedback_service.dart"
      to: "conta_libras_2/lib/core/constants.dart"
      via: "AppConstants.apiBaseUrl"
      pattern: "AppConstants\\.apiBaseUrl"
---

<objective>
Preparar a infraestrutura Flutter para a coleta e envio de feedbacks: adicionar o campo idade ao UserManager, conectá-lo ao FirstAccessScreen, criar AppConstants com a URL da API, e adicionar o pacote http ao pubspec.

Purpose: Esses quatro arquivos são a base que o FeedbackService (Plan 03) depende. Sem a idade no UserManager o payload fica incompleto (viola D-11). Sem o http no pubspec os imports falham. Sem AppConstants a URL fica hardcoded no service.
Output: user_manager.dart com campo age, first_access_screen.dart enviando a idade, constants.dart criado, pubspec.yaml com http.
</objective>

<execution_context>
@Z:/Users/bielr/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Users/bielr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\PROJECT.md
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\STATE.md

<interfaces>
<!-- Contratos extraídos dos arquivos fonte para referência do executor. -->

De conta_libras_2/lib/data/managers/user_manager.dart (estado atual):
```dart
class UserManager extends ChangeNotifier {
  static final UserManager _instance = UserManager._internal();
  factory UserManager() => _instance;
  UserManager._internal();

  String _userName = 'Estudante';
  String _userCategory = 'Estudante de Contábeis';

  String get userName => _userName;
  String get userCategory => _userCategory;

  void setUserData(String name, String category) {
    _userName = name;
    _userCategory = category.isNotEmpty ? category : 'Estudante de Contábeis';
    notifyListeners();
  }
}
```

De conta_libras_2/lib/ui/screens/first_access/first_access_screen.dart (linha relevante):
```dart
// linha 38 — única linha a modificar:
UserManager().setUserData(_nameController.text.trim(), _selectedCategory ?? '');
// _ageController já existe (linha 18) e já é validado como int (linhas 163–170)
```

De conta_libras_2/lib/core/theme/app_colors.dart (padrão de classe utilitária estática):
```dart
class AppColors {
  AppColors._();  // construtor privado
  static const Color primary = Color(0xFF1D3557);
}
```

pubspec.yaml dependencies atual (linhas 30–39):
```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.3.0
  video_player: ^2.9.2
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar campo age ao UserManager e atualizar FirstAccessScreen</name>
  <files>conta_libras_2/lib/data/managers/user_manager.dart, conta_libras_2/lib/ui/screens/first_access/first_access_screen.dart, conta_libras_2/test/user_manager_test.dart</files>
  <read_first>
    - conta_libras_2/lib/data/managers/user_manager.dart (arquivo completo — ler antes de editar)
    - conta_libras_2/lib/ui/screens/first_access/first_access_screen.dart (arquivo completo — ler antes de editar)
    - conta_libras_2/lib/data/managers/theme_manager.dart (referência de padrão singleton ChangeNotifier)
  </read_first>
  <behavior>
    - Test 1: UserManager().userAge retorna 0 antes de qualquer chamada a setUserData
    - Test 2: Após UserManager().setUserData('Ana', 'Estudante', 22), userAge == 22
    - Test 3: Após UserManager().setUserData('Ana', 'Estudante', 0), userAge == 0 (sem fallback para age)
    - Test 4: setUserData com category vazia mantém _userCategory = 'Estudante de Contábeis' (comportamento existente preservado)
  </behavior>
  <action>
    Em user_manager.dart:
    1. Adicionar campo privado int _userAge = 0 após _userCategory (linha 13).
    2. Adicionar getter int get userAge => _userAge; após get userCategory (linha 16).
    3. Alterar assinatura de setUserData para void setUserData(String name, String category, int age).
    4. Adicionar _userAge = age; antes de notifyListeners() no corpo do método.
    Não alterar mais nada — manter o padrão singleton idêntico (per D-11).

    Em first_access_screen.dart, linha 38:
    Substituir a chamada de uma linha por três linhas:
    UserManager().setUserData(
      _nameController.text.trim(),
      _selectedCategory ?? '',
      int.tryParse(_ageController.text.trim()) ?? 0,
    );
    O campo _ageController já existe na linha 18 e já é validado como int no validator (linhas 163–170) — sem outros ajustes necessários.

    Criar conta_libras_2/test/user_manager_test.dart com os 4 casos descritos em behavior, usando flutter_test (já no pubspec dev_dependencies).
  </action>
  <verify>
    <automated>cd conta_libras_2 && flutter test test/user_manager_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - user_manager.dart contém a linha "int _userAge = 0;" após _userCategory
    - user_manager.dart contém "int get userAge => _userAge;"
    - user_manager.dart: assinatura de setUserData é "void setUserData(String name, String category, int age)"
    - first_access_screen.dart linha 38 (aprox): chamada a setUserData passa três argumentos — name, category, e int.tryParse(_ageController.text.trim()) ?? 0
    - flutter test test/user_manager_test.dart retorna todos os 4 testes PASS
  </acceptance_criteria>
  <done>UserManager aceita e expõe age; FirstAccessScreen persiste a idade digitada pelo usuário.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar AppConstants e adicionar http ao pubspec</name>
  <files>conta_libras_2/lib/core/constants.dart, conta_libras_2/pubspec.yaml</files>
  <read_first>
    - conta_libras_2/pubspec.yaml (arquivo completo — ler antes de editar)
    - conta_libras_2/lib/core/theme/app_colors.dart (referência do padrão de classe utilitária estática com construtor privado)
  </read_first>
  <action>
    Criar conta_libras_2/lib/core/constants.dart com:
    - Import: import 'package:flutter/foundation.dart';
    - Classe AppConstants com construtor privado AppConstants._();
    - Getter estático apiBaseUrl usando kDebugMode para diferenciar ambiente:
      static String get apiBaseUrl => kDebugMode
          ? 'http://localhost:8000'
          : 'https://api-conta-libras.railway.app';
    Nota: a URL Railway 'https://api-conta-libras.railway.app' é placeholder — será atualizada após o deploy no Plan 06.
    Usar caminho relativo, sem package alias. Seguir padrão de app_colors.dart.

    Em pubspec.yaml, adicionar após a linha "  video_player: ^2.9.2":
      http: ^1.2.0
    Manter indentação de 2 espaços (padrão das outras dependências).
    Em seguida, rodar: cd conta_libras_2 && flutter pub get
  </action>
  <verify>
    <automated>cd conta_libras_2 && flutter pub get && flutter analyze lib/core/constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo conta_libras_2/lib/core/constants.dart existe
    - constants.dart contém "class AppConstants" e "AppConstants._();"
    - constants.dart contém "static String get apiBaseUrl"
    - constants.dart contém "kDebugMode" e "'http://localhost:8000'"
    - pubspec.yaml contém a linha "  http: ^1.2.0" dentro da seção dependencies
    - flutter pub get termina sem erros (pubspec.lock atualizado com o pacote http)
    - flutter analyze lib/core/constants.dart não reporta erros
  </acceptance_criteria>
  <done>AppConstants disponível para import pelo FeedbackService; http disponível para uso em todo o projeto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| FirstAccessScreen → UserManager | Dado de idade vem do input do usuário; int.tryParse com fallback 0 previne crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | UserManager._userAge | accept | Campo em memória volátil, sem persistência — sem superfície de ataque relevante para TCC |
| T-01-02 | Information Disclosure | AppConstants.apiBaseUrl | accept | URL pública da API; não é segredo. DATABASE_URL nunca vai para o Flutter (D-08) |
| T-01-SC | Tampering | pubspec.yaml (http install) | mitigate | Pacote http é oficial do Dart team (dart.dev); verificado em pub.dev — risco negligenciável |
</threat_model>

<verification>
- flutter test test/user_manager_test.dart — 4 testes PASS
- flutter analyze lib/ — zero erros em user_manager.dart, first_access_screen.dart, constants.dart
- Grep: grep "int get userAge" conta_libras_2/lib/data/managers/user_manager.dart retorna a linha
- Grep: grep "http: \^1.2.0" conta_libras_2/pubspec.yaml retorna a linha
</verification>

<success_criteria>
UserManager.setUserData aceita três argumentos e expõe userAge. FirstAccessScreen passa a idade do campo _ageController. AppConstants.apiBaseUrl existe e diferencia debug/release. Pacote http declarado no pubspec e resolvido pelo pub get.
</success_criteria>

<output>
Criar .planning/phases/01-feedback-api/01-01-SUMMARY.md quando concluído.
</output>
