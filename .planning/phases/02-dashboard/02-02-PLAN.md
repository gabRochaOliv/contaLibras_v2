---
phase: 02-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/database.py
  - dashboard/transforms.py
  - dashboard/charts.py
autonomous: true
requirements:
  - D-03
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "get_connection() usa urlparse e inclui sslmode='require'"
    - "pivot_respostas() produz colunas q4..q29 a partir do JSONB do banco"
    - "build_wide_df() retorna DataFrame com colunas id, nome, idade, categoria, criado_em e qN para cada questão presente"
    - "build_timeline_df() retorna DataFrame com colunas 'data' e 'respostas' agrupadas por dia em America/Sao_Paulo"
    - "chart_medias_por_questao() retorna figura Plotly com range_y=[1,5] e height=500"
    - "chart_comparativo_categoria() retorna figura Plotly com barmode='group' e height=450"
    - "chart_timeline() retorna figura Plotly com markers=True e height=350"
    - "Testes test_pivot_respostas, test_csv_colunas e test_timeline passam (verde)"
  artifacts:
    - path: "dashboard/database.py"
      provides: "get_connection() com urlparse + sslmode='require'"
      contains: "sslmode"
    - path: "dashboard/transforms.py"
      provides: "pivot_respostas(), build_wide_df(), build_timeline_df()"
      exports: ["pivot_respostas", "build_wide_df", "build_timeline_df"]
    - path: "dashboard/charts.py"
      provides: "Funções de criação de figuras Plotly"
      exports: ["chart_medias_por_questao", "chart_comparativo_categoria", "chart_timeline"]
  key_links:
    - from: "dashboard/database.py"
      to: "Supabase Session Pooler"
      via: "psycopg2.connect(host=url.hostname, ...)"
      pattern: "urlparse.*DATABASE_URL"
    - from: "dashboard/transforms.py"
      to: "respostas JSONB"
      via: "df.explode('respostas') + pivot_table()"
      pattern: "pivot_table"
    - from: "dashboard/charts.py"
      to: "dashboard/transforms.py"
      via: "recebe df_wide como argumento"
      pattern: "def chart_.*df_wide"
---

<objective>
Implementar a camada de dados e a camada de gráficos do dashboard — sem UI Streamlit. Inclui a conexão ao banco, as transformações JSONB→tabela e as três funções de criação de figuras Plotly.

Purpose: Separar a lógica testável (transforms.py) da UI (app.py), e verde os testes unitários que foram escritos em estado RED no plano 01.
Output: dashboard/database.py, dashboard/transforms.py, dashboard/charts.py — todos funcionais e com testes passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\PROJECT.md
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-CONTEXT.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-PATTERNS.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-UI-SPEC.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-VALIDATION.md

<interfaces>
<!-- Padrão de conexão existente extraído de api/database.py (CRÍTICO — replicar com sslmode adicionado) -->

From api/database.py (linhas 1–18):
```python
import os
import psycopg2
from dotenv import load_dotenv
from urllib.parse import urlparse

load_dotenv()

def get_connection():
    url = urlparse(os.environ["DATABASE_URL"])
    return psycopg2.connect(
        host=url.hostname,
        port=url.port,
        database=url.path.lstrip("/"),
        user=url.username,
        password=url.password,
        # DASHBOARD DEVE ADICIONAR: sslmode="require"
    )
```

<!-- Schema tabela feedbacks -->
<!--
CREATE TABLE feedbacks (
    id         SERIAL PRIMARY KEY,
    nome       VARCHAR(255)  NOT NULL,
    idade      INTEGER       NOT NULL,
    categoria  VARCHAR(100)  NOT NULL,
    respostas  JSONB         NOT NULL,  -- [{pergunta_id: int, valor: int}]
    criado_em  TIMESTAMPTZ   NOT NULL DEFAULT NOW()
);
-->

<!-- Mapeamento seções IHC (do UI-SPEC.md Section Mapping) -->
<!--
Usabilidade (SUS):    q4  – q12  (9 questões)
Experiência (UX):     q11 – q14  (4 questões, overlap q11-q12 com SUS)
Qualidade Conteúdo:   q15 – q18  (4 questões)
Aprendizado:          q19 – q22  (4 questões)
Aceitação (TAM):      q23 – q26  (4 questões)
Avaliação Geral:      q27 – q29  (3 questões)
ATENÇÃO: verificar sobreposição q11-q12 contra Feedback/questionario_IHC.md
-->

<!-- COLOR_MAP do UI-SPEC.md para charts.py -->
<!--
COLOR_MAP = {
    "Pessoa surda": "#1f77b4",
    "Professor":    "#ff7f0e",
    "Estudante":    "#2ca02c",
    "Intérprete":   "#d62728",
    "Outro":        "#9467bd",
}
-->

<!-- Contratos de gráfico do UI-SPEC.md Chart Contracts -->
<!--
Chart 1 — medias_por_questao: px.bar, x=label questão, y=media, range_y=[1,5], height=500, width="stretch"
Chart 2 — comparativo_categoria: px.bar, barmode="group", x=seção, y=media, color="categoria", height=450
Chart 3 — timeline: px.line, markers=True, x=data, y=respostas, height=350
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Camada de dados — database.py e transforms.py</name>
  <files>
    dashboard/database.py
    dashboard/transforms.py
  </files>
  <read_first>
    - Z:\TCC\Aplicativo\api\database.py — padrão urlparse COMPLETO (linhas 1–18); replicar com sslmode adicionado
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md seções "Pattern 1", "Pattern 2", "Pattern 3" e "Pitfall 1", "Pitfall 6"
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-PATTERNS.md seção "dashboard/database.py" e "Shared Patterns"
    - Z:\TCC\Aplicativo\Feedback\questionario_IHC.md — verificar IDs exatos das perguntas (4 a N) para o mapeamento de seções
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-VALIDATION.md — comandos de teste unitário
  </read_first>
  <action>
Criar `dashboard/database.py` e `dashboard/transforms.py`.

**`dashboard/database.py`:**
Replicar `api/database.py` com as seguintes modificações:
- Remover a importação de `psycopg2.extras.Json` (não necessário — somente leitura)
- Remover a função `insert_feedback()` (somente leitura)
- Adicionar `sslmode="require"` como kwarg em `psycopg2.connect()` — obrigatório para Supabase Session Pooler em produção (RESEARCH.md Pitfall 1)
- Manter: `from dotenv import load_dotenv`, `load_dotenv()`, `urlparse`, padrão `url = urlparse(os.environ["DATABASE_URL"])`

Adicionar também `fetch_feedbacks()` com `@st.cache_data(ttl="5m")`:
- Importar `streamlit as st` e `pandas as pd`
- SELECT: `id, nome, idade, categoria, respostas, criado_em FROM feedbacks ORDER BY criado_em DESC`
- Usar padrão `conn.cursor()` + `cur.description` para extrair nomes das colunas
- Fechar conexão no `finally:` (padrão de api/database.py linhas 24–35)
- Retornar `pd.DataFrame(rows, columns=cols)`

**`dashboard/transforms.py`:**
Implementar três funções puras (sem Streamlit — importável em testes):

1. `pivot_respostas(df: pd.DataFrame) -> pd.DataFrame`:
   - Recebe o DataFrame bruto do banco (campo `respostas` como list de dicts Python)
   - Retornar DataFrame vazio com colunas corretas se `len(df) == 0` (edge case do test_pivot_respostas_vazio)
   - Copiar df; expandir com `df.explode("respostas").reset_index(drop=True)`
   - Extrair `pergunta_id` e `valor` de cada dict em `respostas`
   - Usar `pivot_table(index=["id","nome","idade","categoria","criado_em"], columns="pergunta_id", values="valor", aggfunc="first")`
   - Renomear colunas numéricas para formato `qN` (ex: coluna `4` → `"q4"`)
   - Retornar df_wide com reset_index()

2. `build_wide_df(df: pd.DataFrame) -> pd.DataFrame`:
   - Wrapper para `pivot_respostas` — mesmo resultado, mantido por clareza

3. `build_timeline_df(df: pd.DataFrame) -> pd.DataFrame`:
   - Recebe DataFrame bruto (campo `criado_em` como TIMESTAMPTZ)
   - Converter `criado_em` para datetime com `pd.to_datetime(...).dt.tz_convert("America/Sao_Paulo")`
   - Extrair `.dt.date` e agrupar com `.groupby("data").size().reset_index(name="respostas")`
   - Retornar DataFrame com colunas `data` e `respostas`

Definir constante `SECOES_IHC` (dict) mapeando cada questão ao seu nome de seção — usar mapeamento do UI-SPEC.md Section Mapping, mas verificar IDs exatos contra `Feedback/questionario_IHC.md` antes de fixar. Forma: `{"q4": "Usabilidade (SUS)", "q5": "Usabilidade (SUS)", ...}`.

Definir constante `LABELS_QUESTOES` (dict) mapeando cada `qN` ao texto curto da questão, conforme tabela RESEARCH.md "Mapeamento de Perguntas por Seção". Forma: `{"q4": "q4 — Gostaria de usar com frequência", ...}`.
  </action>
  <verify>
    <automated>cd Z:\TCC\Aplicativo\dashboard && py -3.11 -m pytest tests/test_transforms.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest dashboard/tests/test_transforms.py -x -q` passa (4/4 PASSED)
    - `dashboard/database.py` contém `sslmode="require"` dentro de `psycopg2.connect()`
    - `dashboard/database.py` NÃO contém `psycopg2.connect(os.environ["DATABASE_URL"])` — apenas kwargs separados via urlparse
    - `dashboard/database.py` contém `@st.cache_data(ttl="5m")` acima de `fetch_feedbacks`
    - `dashboard/transforms.py` exporta `pivot_respostas`, `build_wide_df`, `build_timeline_df`
    - `dashboard/transforms.py` contém `SECOES_IHC` (dict com pelo menos as 6 seções)
    - `dashboard/transforms.py` contém `LABELS_QUESTOES` (dict com pelo menos q4 e q29)
    - Nenhum import de `streamlit` em `dashboard/transforms.py` (para manter testabilidade pura)
  </acceptance_criteria>
  <done>database.py e transforms.py criados; test_transforms.py está verde (4/4 PASSED).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Camada de gráficos — charts.py</name>
  <files>
    dashboard/charts.py
  </files>
  <read_first>
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-UI-SPEC.md seções "Chart Contracts" e "Color"
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md seções "Pattern 5", "Pattern 6", "Anti-Patterns to Avoid"
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md seção "Code Examples: Timeline de coleta (D-09)"
  </read_first>
  <action>
Criar `dashboard/charts.py` com as três funções de criação de figuras Plotly. Cada função recebe um DataFrame e retorna um objeto `plotly.graph_objects.Figure`.

Importações necessárias: `import plotly.express as px`, `import pandas as pd`, `from transforms import SECOES_IHC, LABELS_QUESTOES`.

Definir `COLOR_MAP` no topo do arquivo (constante):
```
COLOR_MAP = {
    "Pessoa surda": "#1f77b4",
    "Professor":    "#ff7f0e",
    "Estudante":    "#2ca02c",
    "Intérprete":   "#d62728",
    "Outro":        "#9467bd",
}
```

Definir `COLOR_SEQUENCE_SECOES` (lista de 6 cores para seções no chart 1):
`["#1f77b4","#ff7f0e","#2ca02c","#d62728","#9467bd","#8c564b"]`

**Função 1: `chart_medias_por_questao(df_wide: pd.DataFrame) -> go.Figure`** (per D-07, UI-SPEC Chart 1):
- Selecionar colunas `qN` do df_wide
- Calcular médias: `df_wide[q_cols].mean().reset_index()`, colunas `["questao", "media"]`
- Mapear cada `questao` para seu label curto usando `LABELS_QUESTOES` (fallback para o próprio nome se não encontrado)
- Mapear cada `questao` para sua seção usando `SECOES_IHC`
- Criar DataFrame com colunas `questao_label`, `media`, `secao`
- Usar `px.bar(x="questao_label", y="media", color="secao", color_discrete_sequence=COLOR_SEQUENCE_SECOES, labels={"questao_label": "Questão", "media": "Média (1–5)", "secao": "Seção"}, title="Médias Likert por Questão", range_y=[1, 5])`
- Adicionar linha de referência horizontal em y=3: `fig.add_hline(y=3, line_dash="dot", line_color="gray", annotation_text="Neutro (3)")`
- `fig.update_layout(height=500)`
- Retornar fig (NÃO chamar `st.plotly_chart` aqui — isso é responsabilidade de app.py)

**Função 2: `chart_comparativo_categoria(df_wide: pd.DataFrame) -> go.Figure`** (per D-08, UI-SPEC Chart 2):
- Selecionar colunas `qN` do df_wide
- Calcular média por seção: agrupar questões pela seção (via `SECOES_IHC`), depois groupby `categoria`
- Transformação: para cada questão, atribuir sua seção; groupby `["categoria", "secao"]` + mean das colunas qN da seção
- Usar `px.bar(..., x="secao", y="media", color="categoria", barmode="group", color_discrete_map=COLOR_MAP, labels={"secao": "Seção", "media": "Média (1–5)", "categoria": "Categoria"}, title="Comparativo por Categoria de Usuário", range_y=[1, 5])`
- `fig.update_layout(height=450)`
- Retornar fig

**Função 3: `chart_timeline(df: pd.DataFrame) -> go.Figure`** (per D-09, UI-SPEC Chart 3):
- Recebe DataFrame bruto (não df_wide — precisa do campo `criado_em`)
- Converter `criado_em` para timezone America/Sao_Paulo: `pd.to_datetime(df["criado_em"]).dt.tz_convert("America/Sao_Paulo").dt.date`
- groupby data + count
- Usar `px.line(x="data", y="respostas", markers=True, labels={"data": "Data", "respostas": "Respostas"}, title="Volume de Coleta por Dia", color_discrete_sequence=["#1f77b4"])`
- `fig.update_layout(height=350)`
- Retornar fig

Não usar `use_container_width=True` nem `width="stretch"` aqui — esses parâmetros são de `st.plotly_chart()`, não da figura.
  </action>
  <verify>
    <automated>cd Z:\TCC\Aplicativo\dashboard && py -3.11 -c "from charts import chart_medias_por_questao, chart_comparativo_categoria, chart_timeline; print('OK — charts importado com sucesso')"</automated>
  </verify>
  <acceptance_criteria>
    - `from charts import chart_medias_por_questao, chart_comparativo_categoria, chart_timeline` funciona sem erro
    - `charts.py` contém `COLOR_MAP` com as 5 categorias e seus hexadecimais
    - `charts.py` contém `chart_medias_por_questao` com `range_y=[1, 5]` e `fig.update_layout(height=500)`
    - `charts.py` contém `chart_comparativo_categoria` com `barmode="group"` e `fig.update_layout(height=450)`
    - `charts.py` contém `chart_timeline` com `markers=True` e `fig.update_layout(height=350)`
    - `charts.py` NÃO contém `st.plotly_chart` (não é responsabilidade desta camada)
    - `charts.py` NÃO contém `use_container_width=True` (deprecated per RESEARCH.md Anti-Patterns)
  </acceptance_criteria>
  <done>charts.py criado com 3 funções; importável sem erros; contratos de height/barmode/markers/range_y respeitados conforme UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard/database.py → Supabase Session Pooler | Credenciais de banco via DATABASE_URL (env var) — nunca em código |
| psycopg2 → JSONB | Campo respostas retorna como list Python; acesso por chave dict sem eval de string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | database.py — DATABASE_URL | mitigate | `os.environ["DATABASE_URL"]` lido da env var; `load_dotenv()` carrega `.env` local apenas; `.env` no `.gitignore` existente (fase 1) |
| T-02-03 | Tampering | psycopg2.connect() — Session Pooler | mitigate | `urlparse` + kwargs separados (padrão fixado na fase 1, commit 1269447); `sslmode="require"` adicionado para SSL obrigatório |
| T-02-04 | Tampering | transforms.py — pivot JSONB | accept | Query é SELECT sem parâmetros de usuário; pivot usa dados existentes do banco sem eval de string; risco mínimo |
| T-02-SC | Tampering | pip install | accept | Todos os pacotes auditados [OK] em RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
Após execução deste plano, verificar:

1. `py -3.11 -m pytest dashboard/tests/ -x -q` — deve mostrar 8/8 PASSED (4 de test_auth + 4 de test_transforms)
2. `py -3.11 -c "from dashboard.database import get_connection"` ou dentro da pasta: `py -3.11 -c "from database import get_connection; print('OK')"` — importa sem erro
3. `py -3.11 -c "from charts import chart_medias_por_questao; print('OK')"` — importa sem erro
4. Verificar manualmente que `dashboard/database.py` NÃO contém `psycopg2.connect(os.environ[` como chamada direta
</verification>

<success_criteria>
- `py -3.11 -m pytest dashboard/tests/ -v` mostra 8 PASSED (todos os testes do plano 01 + plano 02)
- database.py tem urlparse + sslmode="require"
- transforms.py exporta pivot_respostas, build_wide_df, build_timeline_df, SECOES_IHC, LABELS_QUESTOES
- charts.py exporta 3 funções de gráfico com contratos de UI respeitados
- Nenhuma credencial commitada
</success_criteria>

<output>
Criar `.planning/phases/02-dashboard/02-02-SUMMARY.md` quando concluído.
</output>
