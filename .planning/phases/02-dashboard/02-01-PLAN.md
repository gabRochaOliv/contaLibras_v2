---
phase: 02-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/requirements.txt
  - dashboard/runtime.txt
  - dashboard/pytest.ini
  - dashboard/.streamlit/config.toml
  - dashboard/tests/__init__.py
  - dashboard/tests/conftest.py
  - dashboard/tests/test_transforms.py
  - dashboard/tests/test_auth.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-07
  - D-08
  - D-09
  - D-11

must_haves:
  truths:
    - "Todos os pacotes do dashboard estão declarados em requirements.txt com versões exatas"
    - "pytest executa sem erros de import (test_transforms.py e test_auth.py)"
    - "Fixtures sample_raw_df produzem DataFrame sem conexão ao banco"
    - "Tema Streamlit está declarado em .streamlit/config.toml com primaryColor #1f77b4"
  artifacts:
    - path: "dashboard/requirements.txt"
      provides: "Dependências Python do dashboard"
      contains: "streamlit==1.58.0"
    - path: "dashboard/pytest.ini"
      provides: "Configuração pytest para dashboard/"
    - path: "dashboard/tests/conftest.py"
      provides: "Fixtures de mock sem banco de dados"
      exports: ["sample_raw_df"]
    - path: "dashboard/tests/test_transforms.py"
      provides: "Testes unitários das transformações JSONB"
      contains: "test_pivot_respostas"
    - path: "dashboard/tests/test_auth.py"
      provides: "Testes unitários de autenticação"
      contains: "test_senha_errada"
    - path: "dashboard/.streamlit/config.toml"
      provides: "Configuração de tema Streamlit"
      contains: "primaryColor"
  key_links:
    - from: "dashboard/tests/test_transforms.py"
      to: "dashboard/transforms.py"
      via: "import from transforms"
      pattern: "from transforms import pivot_respostas"
    - from: "dashboard/tests/test_auth.py"
      to: "dashboard/auth.py"
      via: "import from auth"
      pattern: "from auth import check_password"
---

<objective>
Criar a infraestrutura de configuração e testes do dashboard — arquivos de setup (requirements, runtime, pytest, config.toml) e os testes unitários que validam as transformações de dados e a autenticação antes de qualquer implementação.

Purpose: Satisfazer o requisito Nyquist (VALIDATION.md Wave 0) e garantir que os testes unitários existam e falhem antes que o código de produção seja escrito, criando a rede de segurança para o plano 02.
Output: Infra de testes funcional + arquivos de configuração do dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Z:\TCC\Aplicativo\.planning\PROJECT.md
@Z:\TCC\Aplicativo\.planning\ROADMAP.md
@Z:\TCC\Aplicativo\.planning\STATE.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-CONTEXT.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-PATTERNS.md
@Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-VALIDATION.md

<interfaces>
<!-- Padrão de teste existente extraído de api/tests/ -->

From api/tests/conftest.py:
```python
import pytest
import pandas as pd

@pytest.fixture
def valid_payload():
    return {
        "nome": "Ana", "idade": 22, "categoria": "Estudante",
        "respostas": [{"pergunta_id": 4, "valor": 5}, {"pergunta_id": 29, "valor": 4}],
    }
```

From api/requirements.txt:
```
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0,<3.0.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
pytest>=7.0.0
httpx>=0.24.0
```

<!-- Schema da tabela feedbacks (fonte: CONTEXT.md) -->
<!-- respostas JSONB: [{pergunta_id: int, valor: int}] -->
<!-- Questões IHC: IDs 4–29 (29 questões Likert) — verificar questionario_IHC.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Arquivos de configuração do dashboard</name>
  <files>
    dashboard/requirements.txt
    dashboard/runtime.txt
    dashboard/pytest.ini
    dashboard/.streamlit/config.toml
  </files>
  <read_first>
    - Z:\TCC\Aplicativo\api\requirements.txt — padrão de formato de versões (pin format)
    - Z:\TCC\Aplicativo\api\runtime.txt — conteúdo exato ("python-3.11")
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md seções "Standard Stack" e "Pattern 8: railway.json"
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-UI-SPEC.md seção "config.toml Tokens"
  </read_first>
  <action>
Criar a pasta `dashboard/` na raiz do repositório e os quatro arquivos de configuração:

1. `dashboard/requirements.txt` — listar dependências com versões exatas conforme RESEARCH.md Standard Stack:
   - `streamlit==1.58.0`
   - `plotly==6.8.0`
   - `pandas==3.0.3`
   - `psycopg2-binary>=2.9.9` (mesma pin usada em api/requirements.txt)
   - `python-dotenv>=1.0.0`
   - `pytest>=7.0.0`

2. `dashboard/runtime.txt` — copiar exatamente de `api/runtime.txt`: conteúdo é `python-3.11`.

3. `dashboard/pytest.ini` — configuração mínima pytest:
   - `[pytest]` section
   - `testpaths = tests`
   - `python_files = test_*.py`
   - `python_functions = test_*`

4. `dashboard/.streamlit/config.toml` — criar pasta `.streamlit/` dentro de `dashboard/` e o arquivo com seção `[theme]` usando os tokens exatos do UI-SPEC:
   - `primaryColor = "#1f77b4"`
   - `backgroundColor = "#ffffff"`
   - `secondaryBackgroundColor = "#f0f2f6"`
   - `textColor = "#31333f"`
   - `font = "sans serif"`

Criar também `dashboard/tests/__init__.py` com conteúdo vazio (necessário para pytest reconhecer o pacote).
  </action>
  <verify>
    <automated>py -3.11 -c "import ast; f=open('Z:/TCC/Aplicativo/dashboard/requirements.txt'); content=f.read(); f.close(); assert 'streamlit==1.58.0' in content and 'plotly==6.8.0' in content and 'pandas==3.0.3' in content, 'Versoes faltando'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/requirements.txt` contém exatamente: `streamlit==1.58.0`, `plotly==6.8.0`, `pandas==3.0.3`, `psycopg2-binary>=2.9.9`, `python-dotenv>=1.0.0`, `pytest>=7.0.0`
    - `dashboard/runtime.txt` contém `python-3.11` (uma linha, sem espaços extras)
    - `dashboard/pytest.ini` contém `[pytest]`, `testpaths = tests`
    - `dashboard/.streamlit/config.toml` contém `[theme]` e `primaryColor = "#1f77b4"`
    - `dashboard/tests/__init__.py` existe (pode estar vazio)
  </acceptance_criteria>
  <done>Quatro arquivos de configuração criados; estrutura de pastas dashboard/ e dashboard/tests/ existe.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Fixtures de teste e testes unitários (Wave 0)</name>
  <files>
    dashboard/tests/conftest.py
    dashboard/tests/test_transforms.py
    dashboard/tests/test_auth.py
  </files>
  <read_first>
    - Z:\TCC\Aplicativo\api\tests\conftest.py — padrão de fixture (escopo, estrutura do dict de payload)
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-PATTERNS.md seções "dashboard/tests/conftest.py" e "dashboard/tests/test_transforms.py"
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-VALIDATION.md seção "Per-Task Verification Map"
    - Z:\TCC\Aplicativo\.planning\phases\02-dashboard\02-RESEARCH.md seção "Pattern 3: Pivot do JSONB" e "Pattern 4: Autenticação"
  </read_first>
  <action>
Criar três arquivos de testes que validam comportamentos puros (sem banco, sem Streamlit):

**`dashboard/tests/conftest.py`:**
Fixture `sample_raw_df` — retorna `pd.DataFrame` com 2 linhas simulando retorno do banco. Cada linha deve ter campos: `id` (int), `nome` (str), `idade` (int), `categoria` (str), `respostas` (list de dicts `{"pergunta_id": int, "valor": int}`), `criado_em` (str ISO 8601 com timezone). Usar os dados do PATTERNS.md:
- Linha 1: id=1, nome="Ana", idade=22, categoria="Estudante", respostas com pergunta_id=4 valor=5 e pergunta_id=5 valor=4, criado_em="2026-06-01T10:00:00+00:00"
- Linha 2: id=2, nome="João", idade=30, categoria="Professor", respostas com pergunta_id=4 valor=3 e pergunta_id=5 valor=2, criado_em="2026-06-02T11:00:00+00:00"

Fixture `sample_raw_df_vazio` — retorna `pd.DataFrame` vazio com as mesmas colunas (para testar empty state).

**`dashboard/tests/test_transforms.py`:**
Importar de `transforms` (sem prefixo de pacote — pytest roda de dentro de `dashboard/`). Os testes devem ser escritos ANTES da implementação de `transforms.py` — eles vão falhar inicialmente, o que é correto.

- `test_pivot_respostas(sample_raw_df)`: importa `pivot_respostas` de `transforms`; chama `pivot_respostas(sample_raw_df)`; assert `"q4" in df_wide.columns`; assert `"q5" in df_wide.columns`; assert `len(df_wide) == 2`.
- `test_csv_colunas(sample_raw_df)`: importa `pivot_respostas` de `transforms`; chama `pivot_respostas(sample_raw_df)`; assert que `{"id", "nome", "idade", "categoria", "criado_em", "q4", "q5"}` é subconjunto de `set(df_wide.columns)`.
- `test_timeline(sample_raw_df)`: importa `build_timeline_df` de `transforms`; chama `build_timeline_df(sample_raw_df)`; assert que o DataFrame resultado tem coluna `"data"` e coluna `"respostas"`; assert `len(resultado) == 2` (2 datas diferentes nas fixtures).
- `test_pivot_respostas_vazio(sample_raw_df_vazio)`: importa `pivot_respostas` de `transforms`; chama com DataFrame vazio; assert `len(resultado) == 0` (não deve levantar exceção).

**`dashboard/tests/test_auth.py`:**
Os testes de autenticação NÃO podem importar Streamlit (causaria erro por falta de contexto). Testar apenas a lógica de comparação de senha via `hmac.compare_digest` diretamente, sem a função `check_password` completa (que depende de `st.session_state`).

- `test_hmac_senha_correta()`: assert `hmac.compare_digest("senha123", "senha123") is True`
- `test_hmac_senha_errada()`: assert `hmac.compare_digest("errada", "senha123") is False`
- `test_hmac_senha_vazia()`: assert `hmac.compare_digest("", "senha123") is False`
- `test_hmac_timing_safe()`: importar `time`; medir 3 iterações de `compare_digest("x"*100, "y"*100)` e `compare_digest("x"*100, "x"*100)`; assert que ambas completam (não testa timing diretamente — só confirma que não levanta exceção e retorna bool).

Adicionar no topo de `test_auth.py`: `import hmac` (stdlib — sem instalação).
  </action>
  <verify>
    <automated>cd Z:\TCC\Aplicativo\dashboard && py -3.11 -m pytest tests/test_auth.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.11 -m pytest dashboard/tests/test_auth.py -x -q` passa (4 testes PASS) — esses testes não dependem de código não-existente
    - `py -3.11 -m pytest dashboard/tests/test_transforms.py -x -q` falha com `ModuleNotFoundError: No module named 'transforms'` (correto — transforms.py ainda não existe)
    - `conftest.py` contém fixture `sample_raw_df` com `@pytest.fixture` e retorna `pd.DataFrame`
    - `conftest.py` contém fixture `sample_raw_df_vazio` retornando DataFrame com colunas mas sem linhas
    - `test_transforms.py` tem os 4 testes listados; nenhum importa Streamlit
    - `test_auth.py` tem os 4 testes listados; usa apenas `import hmac` (stdlib)
  </acceptance_criteria>
  <done>Infraestrutura de testes criada: test_auth.py passa; test_transforms.py falha com ImportError (red state esperado antes do plano 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → transforms module | Testes importam transforms.py — quando o módulo existir, os asserts validam o contrato |
| test runner → auth module | test_auth.py testa hmac.compare_digest diretamente (stdlib) — sem superfície de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm/pip/cargo installs | accept | Todos os pacotes auditados via slopcheck [OK] em 2026-06-05 (RESEARCH.md Package Legitimacy Audit) — sem pacotes [ASSUMED] ou [SUS] |
| T-02-SC | Tampering | pip install (requirements.txt) | accept | slopcheck [OK] para todos os 5 pacotes; audit table presente e completa em RESEARCH.md |
</threat_model>

<verification>
Após execução deste plano, verificar:

1. `py -3.11 -m pytest dashboard/tests/test_auth.py -v` — deve mostrar 4 PASSED
2. `py -3.11 -m pytest dashboard/tests/test_transforms.py -v` — deve mostrar ImportError (expected red)
3. `cat dashboard/requirements.txt` — deve conter `streamlit==1.58.0`
4. `cat dashboard/.streamlit/config.toml` — deve conter `primaryColor = "#1f77b4"`
</verification>

<success_criteria>
- Pasta `dashboard/` criada com estrutura correta (requirements.txt, runtime.txt, pytest.ini, .streamlit/config.toml, tests/)
- `test_auth.py` passa (4/4) — esses testes são independentes de código não-existente
- `test_transforms.py` está em estado RED (ImportError) — aguardando plano 02
- Nenhuma credencial ou DATABASE_URL nos arquivos commitados
</success_criteria>

<output>
Criar `.planning/phases/02-dashboard/02-01-SUMMARY.md` quando concluído.
</output>
